# 客户订单与需求计划关联方案

## 1. 背景与目标

### 1.1 现状

- 现有系统每天两次定时计算缺货，按供应商聚合生成需求计划和采购单。
- 同时存在用户手动创建的需求计划。
- 采购单到货、取消等状态变动频繁。
- 业务目标是清晰掌握每个订单商品的 E2E 履约时效。

### 1.2 目标

**将“需求生成”与“履约关联”任务解耦执行。** 任务 B（关联任务）在任务 A（生成任务）结束后 10 分钟启动，确保关联计算时能读取到当轮最新的供给数据。

---

## 2. 数据模型

### 2.1 CCO 商品行新增字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `inventory_qty` | integer | 当前可用库存覆盖量 |
| `demand_plan_links` | JSON | `[{demand_plan_id, qty}]` — 关联的需求计划明细 |

### 2.2 核心设计原则

> **“跑批即真相”：** 所有关联字段仅由任务 B 在执行时全量重写。任务 A、手动确认、PO 变更等操作均不实时触发关联修改，确保数据的一致性与逻辑的纯粹性。

---

## 3. 定时任务执行逻辑（双任务架构）

### 任务 A：需求自动生成任务
**触发时机**：每天定时（如 09:00 / 21:00）。
**核心逻辑**：
1.  **筛选**：找出所有支付且未发货的订单缺口。
2.  **分流**：
    - `batch` SKU：计算 `总需求 - 库存 - 在途`。
    - `sell_one_buy_one` SKU：全量计入缺口。
3.  **生成**：按供应商聚合缺口，生成**需求计划（草稿）**及对应的**采购单**。

---

### 任务 B：履约关联计算任务
**触发时机**：在任务 A 结束 **10 分钟后** 启动（如 09:10 / 21:10）。
**核心逻辑**：

**Step 1：确定计算范围并清空快照**
- **订单范围**：支付状态为“已支付” **且** 发货状态为“未发货”或“部分发货”的客户订单。
- **排除规则**：排除商品状态为“海外仓”的 SKU 数量。
- **操作**：将符合上述范围 CCO 行的 `inventory_qty` 设为 0，`demand_plan_links` 设为 `[]`。

**Step 2：构建“终极供给池”**
- **可用库存池**：读仓库实时库存。
- **在途 PO 池**：
    - **范围**：需求管理中的“采购需求”与“生产需求”。
    - **全局排除**：需求单状态为“待确认”或“已完结”的单据。
    - **行级排除**：需求单内 SKU 状态为“已完结”的行。
    - **包含**：包含 10 分钟前任务 A 刚刚生成的 PO（需符合上述状态要求）。
- **排序**：PO 按预计到货时间 ASC 排序。

**Step 3：FIFO 重新分配（按商品类型匹配）**
- 按 CCO 创建时间 ASC 排序，依次消耗供给池。
- **商品类型匹配逻辑**：在分配在途 PO 时，需严格对应以下映射关系：
    - **外部商品 + 现货款**：匹配需求单中商品类型为“现货”的资源。
    - **POD 款**：匹配需求单中商品类型为“POD”的资源。
    - **AIGC 款**：匹配需求单中商品类型为“自研款”的资源。
- **分配优先级**：
  1.  `batch` 策略：先吃可用库存（写 `inventory_qty`），再根据匹配规则吃在途 PO（写 `demand_plan_links`）。
  2.  `sell_one_buy_one` 策略：跳过库存，直接根据匹配规则吃在途 PO。

**Step 4：写入结果**
- 批量更新 CCO 商品行字段，完成本轮关联。

---

## 4. 关键场景走查

### 4.1 自动生成后立刻关联（无空白期）

```
09:00 任务 A：
  发现订单 A 缺 30 件，生成 DR-1。
  
09:10 任务 B：
  Step 2 构建池时，发现 DR-1 已在在途池中。
  Step 3 分配时，直接将 DR-1 分配给订单 A。
  结果：订单 A.demand_plan_links = [{DR-1: 30}]。
```

### 4.2 手动创建的采购单

```
用户在 14:00 手动创建了 PO-manual。
21:10 任务 B 启动：
  在途池包含 PO-manual。
  按 FIFO 规则，PO-manual 被分配给排队中的订单 C。
```

### 4.3 PO 部分到货/取消

```
PO-1 原始 50 件，实际只到 20 件，其余 30 件取消。
下一次任务 B 执行：
  Step 2：计算 PO-1 余量 = 50 - 20(到) - 30(取消) = 0。
  结果：PO-1 退出供给池，订单不再关联 PO-1，时效自动切换为下一张 PO。
```

---

## 5. 验收标准

| # | 标准 |
|---|------|
| AC1 | 任务 B 必须在任务 A 明确结束后延迟 10 分钟执行。 |
| AC2 | 任务 B 执行后，CCO 的 `demand_plan_links` 必须包含任务 A 当轮生成的 PO。 |
| AC3 | 无论供给如何变动（到货、取消、手动增单），CCO 关联关系始终以最近一次任务 B 为准。 |
| AC4 | `sell_one_buy_one` SKU 的 `inventory_qty` 在任务 B 执行后必须恒为 0。 |
| AC5 | 查询接口返回的 E2E 时效必须是基于 `demand_plan_links` 中各单据预计到货时间的加权平均值。 |
