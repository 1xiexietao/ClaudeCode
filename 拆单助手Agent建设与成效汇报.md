# 拆单助手 Agent 建设与成效汇报

> 项目汇报 · 2026 Q3

## 一、项目背景

### 业务现状
- 动销订单来源于 Shopify
- 国内发货商品先采购回自有仓库，使用第三方 SaaS 系统**易仓**承接入库和出库流程
- 商品来源多元化：供应商商品（国内发货 + 海外代发 + 国内代发）/ 其他平台海外代发（CJ、Trendsi、等）

### 业务痛点
- 订单商品仓发地不同，均需**人工一单一单手动拆单**才能履约，效率极低
- 拆单规则复杂：先按**国内 / 海外**拆，海外再按**供应商**拆，多维度叠加
- 国内订单需按**到仓时间**拆单：创建 X 天后检查入库状态，已入仓先发，避免整单等待
- 海外代发全靠**线下表格人工流转**，易出错，难追溯；易仓不支持以上规则自动处理

## 二、项目目标

- 按仓发地（国内 / 海外）和供应商**自动分组拆单**，替代人工线下表格流转
- 超期未入仓订单按易仓实时库存**自动触发二次拆单**，已入仓先发，减少等待
- 订单数据异常或系统故障，**自动推送钉钉告警**，无需人工轮询排查
- 预售、POD、海外转国内等复杂场景，ERP 页面支持**人工手动拆单兜底**

## 三、人效分析

### 处理效率提升约 6 倍

#### 效率对比

| 场景 | 耗时 | 说明 |
|------|------|------|
| **人工操作** | 1.8 min | 单订单处理耗时：① 查看 Shopify 发货单结构 ② 判断仓发地和供应商归属 ③ 人工分组并记录 ④ 执行拆单操作 |
| **特殊场景** | 1 min | 占比 30% · 仍需人工处理（如海外转国内发、预售、POD 的二次拆单） |
| **Agent 自动化** | ≈ 0.3 min | 加权平均耗时（含 30% 特殊场景）：70% 常规 → 全自动，≈ 0；30% 特殊 → 仍需人工，1 min |

#### 效率提升

**1.8 min → 0.3 min = 约 6x 提速**

#### 效率对比图表

- **人工**：1.8 min（72% 宽度）
- **Agent（加权）**：≈ 0.3 min（12% 宽度）

#### 量化收益

| 指标 | 人工 | Agent | 节省 |
|------|------|-------|------|
| **单订单处理** | **1.8 min** | **≈ 0.3 min** | **1.5 min** |
| **日均 100 单** | **约 3 h** | **≈ 0.5 h** | **约 2.5 h** |
| **月均（22 工作日）** | **约 66 h** | **≈ 11 h** | **约 55 h** |

#### 计算方式

70% 常规场景 Agent 全自动（≈ 0）+ 30% 特殊场景仍需人工 = 加权平均 **≈ 0.3 min** / 单（业务测算值）

#### 后续规划

当前约 **30%** 的特殊场景（POD、预售、海外转国内发等）仍需人工处理。后续将持续完善 POD、预售、二次拆单等规则，推动这部分场景向 Agent 自动拆单迁移，**进一步优化人工介入比例**。

## 四、整体方案

### 设计原则

- **单源拆分**：拆单决策统一在 OFP + Agent 完成，易仓只负责发货，不参与拆单 / 合单
- **职责边界清晰**：Agent 处理规则明确的自动化场景；ERP 页面处理需人工裁决的手动拆单
- **可追溯**：OFP 记录完整操作日志（时间 + 操作人 + 内容），Agent 输出附带环境 + 版本号
- **主动监控**：Agent 自动巡检，发现后端异常（403/500）或订单商品数据异常时，实时推送钉钉告警

### 系统角色矩阵

| 系统 | 角色 | 职责 |
|------|------|------|
| **Shopify** | 订单源 | 原始订单 · 拆分 API · 物流回写 |
| **订单履约平台（OFP）** | 调度中枢 | 订单接收 · 拆单队列 · 手动拆单 · 手动标发 · 子单存储 · 操作日志 |
| **拆单助手 Agent** | 拆单主体 | 轮询 · 分组决策 · 下发 · 拉单 · 告警 |
| **易仓 ERP** | 发货执行 | 入库 · 出库 · 发货 · **不拆单/不合单** |

### 数据流转关系

#### 主流程（横向）

```
Shopify → (① Webhook 推送订单) → OFP → (④ 推送已拆订单) → 易仓 ERP
   ↑                           ↓                           ↓
   ← (⑦ 回填物流单号) ← (⑤/⑥ 发货通知/单号) ← (④ 推送已拆订单) ←
```

#### 纵向交互（OFP ↔ Agent）

- **② 轮询/决策**：OFP → Agent（定时轮询待处理订单）
- **③ execute**：Agent → OFP（执行拆单并回写结果）

### Agent & ERP 能力

#### Agent — 结构和能力（五层架构）

##### ① 触发层 — Agent 怎么"醒"
- **钉钉 @机器人** — 人工触发，即时响应
- **Cron 定时轮询** — 每 10 分钟自动巡检
- **事件触发** — 拆单完成后自动触发易仓同步

##### ② 感知层 — Agent 怎么"看"
- **poll_split_queue** — 轮询待处理订单队列
- **get_split_context** — 获取订单详情与发货单上下文

##### ③ 决策层 — Agent 怎么"想"（核心层）
> 只负责"想"，不负责"做"

- **规则判断** — 按 warehouseType + supplierId 决定分组策略，缺值自动拦截
- **拆单决策** — 判断是否需要拆单、拆成几笔、按什么维度拆
- **二次拆分** — 超 7 天未履约子单，按易仓实时库存判断是否二次拆分
- **发货拉取决策** — 判断 WAITING_FULFILL 订单是否已从易仓发货，决定是否拉取单号

##### ④ 执行层 — Agent 怎么"做"（MCP 工具 · 纯执行，不含业务逻辑）
- **execute_split_order** — 执行拆单并下发至履约平台
- **pull_domestic_fulfillment** — 拉取易仓发货单号并回填
- **stock_resplit** — 按库存执行二次拆分

##### ⑤ 保障层 — Agent 怎么"稳"
- **主动故障告警** — 后端 403/500 或订单商品数据异常时，自动推送钉钉群，附带错误码与日志片段
- **版本控制** — Git Tag 语义化版本管理，输出标注环境 + 版本号
- **多环境隔离** — OFP_ENV 环境变量切换 QA / Prod，API Key 安全注入
- **操作日志** — 完整记录时间 + 操作人 + 内容，可追溯

#### 为什么这样拆？（设计价值）

##### 传统模式
```
规则变更 → 改代码 → 发版本 → 功能回归测试 → 上线
周期长 · 成本高 · 风险大
```

##### Agent + MCP 模式
```
规则变更 → 改 Agent 规则 + 测试 → 上线
无需改代码 · 无需发版本
前提：新增规则所需字段已有，无需新增字段查询
```

**核心优势：**
- ✓ 规则 Agent 化，适应度高
- ✓ 决策与执行分离，职责清晰
- ✓ 规则变更 = 配置变更，非代码变更
- ✓ 上线周期大幅缩短

#### ERP 迭代 — 能力

- **拆单结果展示** — Agent 拆单结果按分组可视化呈现，支持查看每笔子单明细
- **手动拆单** — 预售、POD、海外转国内等特殊场景，页面支持人工手动拆单兜底
- **标记发货** — 页面手动标记发货/批量导入发货，处理 Agent 未覆盖的场景
- **操作日志** — 完整记录时间 + 操作人 + 内容，所有操作可追溯

> 手动拆单涉及 Shopify + OFP + 易仓 **3 平台 · 8 个 API** 链式调用

## 五、Agent 自动拆单流程

> 订单进入待处理队列后，Agent 每 10 分钟自动巡检并完成拆单

### 前置条件
订单履约平台接收 Shopify 订单后，写入**待处理队列**，状态标记为"待处理"

### 流程步骤

1. **拉取待处理订单** — 从待处理队列中筛选所有"待处理"状态的订单，逐笔遍历
2. **查询订单详情** — 根据订单号向 Shopify 查询完整的发货单信息
3. **校验发货单状态** — 检查每张发货单的状态，**可处理**的进入下一步，已关闭/已发货的跳过并记录
4. **判断是否需要拆单**
   - 海外仓多供应商混合 → 拆分
   - 单一类型 → 直接记录
5. **执行拆单并回写结果** — 调用 Shopify 拆单接口完成拆分，将拆单结果写回订单履约平台（支持页面查看、手动拆单、手动标记发货）
6. **标记处理结果**
   - ✓ 成功 → 标记"已处理"
   - ✗ 失败 → 记录重试，保持"待处理"
7. **异常告警** — 同一订单失败超过 3 次 → 标记为"失败"，**推送钉钉告警**，附带错误详情

## 六、同步易仓及发货状态回传

> 拆单完成后，国内仓订单自动同步易仓；Agent 定时监听发货状态，获取物流单号并回写 Shopify

### 阶段一：同步易仓（拆单完成后自动触发）

1. **获取国内仓子订单** — 拆单完成后，Agent 自动获取拆分出的国内仓子订单列表
2. **同步订单至易仓** — 调用易仓同步接口，推送订单信息及商品明细
3. **记录同步状态** — 子订单标记为**"已同步"**，同时记录易仓订单号，便于后续追踪
4. **易仓处理发货** — 订单进入易仓标准流程：**入库 → 出库 → 发货**

### 阶段二：发货状态回传（Agent 定时任务 · 每 10 分钟）

5. **查询易仓发货状态** — 定时向易仓查询订单列表，筛选**已发货**的订单
6. **匹配履约平台子订单** — 通过订单号将易仓发货信息与履约平台中的子订单进行匹配
7. **提取物流信息** — 从易仓返回数据中提取**物流单号**和**物流公司**
8. **更新履约平台状态** — 子订单标记为**"已发货"**，写入物流单号和物流公司
9. **回写 Shopify 物流信息** — 调用 Shopify 发货接口，将物流单号和公司回写至平台
10. **买家可查看物流** — Shopify 订单标记为已发货，买家端实时可见物流追踪信息

### 流程说明
- **同步易仓**：拆单完成后自动触发（国内仓订单）
- **发货回传**：Agent 定时任务（每 10 分钟）

## 七、项目时间线

### 20 个工作日 · 6 阶段交付

| 阶段 | 时间 | 工期 | 说明 |
|------|------|------|------|
| **初步调研** | 06.09 - 06.10 | 2 天 | |
| **方案选型** | 06.11 - 06.17 | 5 天 | 锁定方案三 |
| **方案落地** | 06.18 - 06.25 | 5 天 | |
| **Agent 能力建设** | 06.23 - 06.30 | 6 天 | 提前介入，与方案落地并行 3 天 |
| **ERP 迭代 + Agent 增强** | 07.01 - 07.06 | 4 天 | |
| **生产交付** | 07.07 | 1 天 | |

#### 说明
- **方案选型 + 方案落地（10 个工作日）**期间，持续探索多种技术路径：Agent 模拟易仓手动拆单操作、Shopify Webhook 自动触发执行等方案，同步与易仓技术团队沟通订开 API 接口。
- 同期 ERP 并行迭代两个版本（6 月 22 日、6 月 30 日），受排期影响，方案阶段需与其他迭代内容穿插进行。

## 八、经验复盘

### 为什么要用 Agent + ERP 迭代的方式？

#### 手动拆单复杂度高，不适合 Agent 化

- **🔗 跨平台事务一致性**
  涉及 Shopify + OFP + 易仓，8 个 API 链式调用，任一环节失败需回滚前序操作，需 Saga / 补偿机制

- **🖱️ 依赖 UI 交互**
  操作人员需在页面上点选 SKU、预览分组结果，Agent 对话式交互无法承载此类 UI 操作

- **⚙️ 确定性编排优于 AI 推理**
  8 个 API 的调用顺序、超时、重试策略，后端代码实现比 Prompt 推理更可靠、更经济

#### ERP 迭代的长期价值

- **🌐 为海外仓线上化奠定基础**
  手动拆单沉淀的跨平台 API 调用能力（Shopify 拆单、易仓订单管理、履约数据同步），可直接复用于海外仓订单线上化场景

- **🏗️ 拆单引擎可扩展**
  ERP 端沉淀的拆单引擎和平台连接层，为后续全链路自动履约（含海外仓）奠定技术基础

### 手动拆单 · 跨平台 API 调用链路

1. **查询易仓订单** `易仓` — 确认待拆订单在易仓中的状态
2. **调用 Shopify 拆单 API** `Shopify` — 执行 Shopify 侧发货单拆分
3. **查询 Shopify 发货单状态** `Shopify` — 确认拆分结果，获取新发货单信息
4. **履约系统写入拆单结果** `OFP` — 将拆单后的子单数据写入订单履约平台
5. **调用易仓取消原订单** `易仓` — 在易仓中取消拆分前的原始订单
6. **调用易仓创建订单** `易仓 ×2` — 按拆分后的子单分别创建新订单（两次调用）
7. **修改履约订单信息** `OFP` — 更新履约平台中的订单状态与关联信息

---

*文档结束*
